ultralytics>=8.3
torch>=2.2
streamlit>=1.40
numpy>=1.26
pillow>=10.0
rfdetr>=1.8
supervision>=0.20
# CLIP backbone for YOLO-World (open-vocabulary); pre-installed to avoid
# ultralytics' runtime AutoUpdate delay on first inference.
ftfy
clip @ git+https://github.com/ultralytics/CLIP.git
pytest>=8.0
